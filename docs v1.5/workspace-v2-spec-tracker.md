# Workspace v2 / Project Chat Model / Onboarding v1.5 — Spec vs. Backend vs. Frontend Tracker

**Purpose:** living reference reconciling three Superhuman Docs specs against what `back-end-test` (branch `test`, commit `51a38d6`) actually implements and what the frontend (`front-end/`) currently does. Every backend claim below is sourced from reading `D:\WJP Souvenir\back-end-test` directly — file:line noted — not from the docs or from assumption.

**Sources:**
- [Workspace Model v2 — Entities, Billing, Credits](https://docs.superhuman.com/d/Product-Team-Doc_dgtnRlALWv3/Workspace-Model-v2-Entities-Billing-Credits_suM6tOKn)
- [Project Chat Model](https://docs.superhuman.com/d/Product-Team-Doc_dgtnRlALWv3/Project-Chat-Model_suRwrfIl)
- [Onboarding V1.5](https://docs.superhuman.com/d/Onboarding-Settings-Connectors-V1-5-Implementation-Flow_dgfHvG_bkGD/Onboarding-V1-5_suY3RN_R) + local `onboarding-v1.5-flow.md`
- [Connectors — Connections v1 UX flow](https://docs.superhuman.com/d/Connectors_deAyH5BPP5a/Connectors_surqivP-)
- Storybook reference: `may-day-final/src/stories/teams/ConnectorLibraryV1.stories.tsx` (S1–S22 end-to-end connector flow)

**Status legend:** ✅ Done/matches · ⚠️ Partial / open but non-blocking · ❌ Open gap · 🔒 Held (explicitly deprioritized by user) · — Not evaluated this pass

**Last updated:** 2026-08-29

---

## 1. Onboarding

| # | Spec requirement | Frontend (current) | Backend dependency | Status |
|---|---|---|---|---|
| O1 | A1a: choice between "Continue with Slack" and "Continue setting up" (web) | `onboarding/setup/page.tsx` — implemented, has try/catch + toast on `getSlackInstallUrl()` failure | `build_install_url`/`completeInstall` (`services/slack/service.py:529,541,594`) call `require_admin_org()` — **requires an existing org membership**, 403s otherwise. Install also never creates an org or fills onboarding fields — only attaches a bot token to an existing org. This is the very first onboarding screen, before any workspace exists, so a brand-new signup's click will 403 before ever reaching Slack | ❌ Open — backend requires an org that doesn't exist yet at this step |
| O2 | A1b: Workspace name (required) + size (Just Me default); empty-field error; Next disabled until valid | `workspace/page.tsx` — inline error ✅, `nextDisabled` now wired (fixed 2026-08-29) | `createOrganization`/`updateOnboarding` — soft-fail: toast shown but page **always advances** even if the save fails | ✅ Validation fixed · ⚠️ soft-fail-then-advance behavior still open, not yet fixed |
| O2a | **Reproducible bug found and fixed, 2026-08-29:** Back then Next again on the workspace step threw "You already belong to an organization" every time | Root cause: `GET /users/me` has **no `org_id` field at all** in its response schema (`services/users/schemas.py`) — `user.orgId` is permanently `null`, and the `refreshUser()` call that the old code relied on to populate it after `createOrganization()` could never actually do so. So a second visit to this step always saw `orgId` as still null and tried to create a **second** organization, which the backend correctly rejects | `workspace/page.tsx` now resolves the real org id via `listOrganizations()` first (falling back only when `user.orgId` is unset) — the same pattern `invite/page.tsx` and `onboarding/connectors/page.tsx` already used for this exact known gap. Removed the now-provably-ineffective `refreshUser()` call and its misleading comment | ✅ Fixed |
| O3a | **Deliberate spec reversal, 2026-08-29:** an "AI tone" field (Direct/Balanced/Warm) was added to `/onboarding/profile`, even though `onboarding-v1.5-flow.md`'s Case A1 section says verbatim *"exactly these 7 pages, nothing else... no tone step... not part of A1."* This was surfaced to the user before implementing (flagged the explicit exclusion), and adding it anyway was the confirmed choice, not an oversight | N/A — pure frontend/product decision, no backend dependency; `updateOnboarding()` already had a `TONE_API_MAP` (`lib/api/user.ts:510`) mapping `Direct/Balanced/Warm` → the real backend enum (`concise/balanced/empathetic`) left over from the old `onboarding/tone/page.tsx`, so no new backend wiring was needed | New `tone`/`setTone` state added to `workspace-onboarding-context.tsx`; `profile/page.tsx` gained a `ToneSelect` dropdown (same `Dropdown`/`DropdownFloat` pattern as the existing `RoleSelect`) rendered below Role, saved via the same `updateOnboarding()` call | ✅ Added — if this flow is ever re-audited against the doc, this row explains the intentional deviation from O1's Case-A1 "exactly these 7 pages" framing |
| O3 | A1c: First/Last name (required), Role dropdown (Founder/Marketer/Designer/Engineer/Operator/Student-Researcher/**Other disabled**) | `profile/page.tsx` — role list matches exactly. First shown as `disabled` (2026-08-29), then per explicit user request (later same day) **removed entirely** from both the visible list and the `WorkspaceOnboardingRole` type — no longer selectable in any form. Since this page is shared with A2's "Join a workspace" flow, the removal applies there too. Inline error ✅, `nextDisabled` wired ✅ | Same soft-fail-then-advance pattern as O2 on `updateUser` failure | ✅ Role list + validation fixed · ⚠️ soft-fail still open |
| O4 | A1d: Email invites — comma/newline split, tag/chip UI with remove ✕, skippable, "secure email verification" (format validation) | `invite/page.tsx` — rewritten to chip UI (Badge + remove button, `ChipInput` for entry), regex email validation added, skip preserved (fixed 2026-08-29) | `updateOnboarding({onboarding_completed:true})` — correctly **blocks** navigation on failure (unlike O2/O3) — this page is the most robust of the three | ✅ Fixed |
| O5 | A1d: "Add Souvenir to Slack" modal on completion | `_components/add-to-slack-modal.tsx` — implemented, try/catch + toast | `getSlackInstallUrl()` — same org-required constraint as O1, but by this point in A1 the org already exists (created at O2), so this instance should work | ✅ Verified fine |
| O6 | A2: "Join a workspace" (org name/member count/avatar stack/Join button) | `join/page.tsx` — implemented correctly; calls `getTeamInviteOnboarding`/`acceptTeamInvite` **after** login, so does not hit the O8 auth issue | Uses `/org-invite/{id}` authenticated — fine | ✅ Functionally correct · minor: no "Try again" button on initial load failure |
| O7 | A2: profile + Slack-modal screens reuse A1's components | Confirmed identical instances, no new code needed | — | ✅ Matches |
| O8 | B1/B2: pre-login invite landing, Sign in vs. Sign up | `app/invite/[inviteId]/page.tsx` — implemented, graceful error state, no retry button | `GET /org-invite/{invite_id}` requires `Depends(get_current_user)` (`services/organizations/router.py:492`) — **not** anonymous-accessible. This page calls it before login, so it will 401 for genuinely logged-out visitors, always landing on the "couldn't find invite" error state | 🔒 Held — confirmed real blocker, explicitly on hold per user (2026-08-29) |
| O9 | B1 vs B2 selection signal — "does this invited email already have a Souvenir account" | `?existingAccount=1` query-param workaround, defaults to B2 (Sign up) when missing/invalid | `InvitePreview` schema (`services/organizations/schemas.py:300`) has no such field — doesn't even return the invitee's email | ❌ Open — no backend signal exists |

---

## 2. Chats

| # | Spec requirement | back-end-test reality | Frontend (current) | Status |
|---|---|---|---|---|
| CH1 | Every chat is private by default; **even inside a shared project, invisible to other members — no exceptions** | **Violated.** `chat_readable_by_user` (`services/chat/repository.py:78`) grants read access to any chat linked to a project the caller can see, with **no check on that chat's own private/shared flag**. Since "project visible to user" = any org-wide shared project (`shared_visibility_arm`, `organizations/roles.py:31` — org-wide, no per-project membership to narrow it), **any org member can read the full message content of any other member's "private" chat** if it's linked to any shared project | Frontend doesn't add exposure beyond calling the existing endpoint — this is a pure backend authorization hole | ❌ **Open — live privacy bug**, not just a migration gap. Highest-severity item on this whole tracker |
| CH2 | (Supporting leak) Project chat listing should only surface chats the viewer is allowed to see | `get_project_chats` (`services/projects/repository.py:204`, backs `GET /projects/{id}/chats`) returns **every** chat linked to the project from **every** user — title, owner, timestamps, message count — with **no visibility filter at all** | `projects-context.tsx`'s `loadProjectChats()` calls this endpoint and stores the full (leaky) result in `chats` state, used by the actual chat-detail page's `activeChatRecord` lookup. The project *overview* page avoids rendering it directly for shared projects (uses own-only `listChats()` for the Personal/Published tabs instead), but the underlying leaky fetch still happens and backs the chat-detail route | ❌ Open — same root cause as CH1 |
| CH3 | Chat sharing is explicit and strictly view-only (no continue/edit/branch/delete by the recipient) | Chat-shares `fork` creates a brand-new chat the recipient owns; there is no endpoint to write into the original shared chat | `ChatShareOverlay.tsx`/`chat-shares.ts`: "editable" mode only grants a fork button, not in-place editing. **Verified compliant** — corrected a stale finding from the 2026-08-20 audit that had flagged this as a violation | ✅ Verified compliant (2026-08-29) |
| CH4 | Only the original creator can unshare a previously shared chat | `set_resource_visibility` (`services/organizations/service.py:1465`) requires `owner_id == user_id` or 403s — applies to `kind == "chat"` too | The publish/unpublish toggle (`handlePublishToggle`, `project/[id]/page.tsx`) is only ever rendered for the viewer's own chats (`teamChats`, sourced from own-chats-only `listChats()`) | ✅ Compliant |
| CH5 | What happens to chats when a project is deleted: all chats within it (private and shared) should be deleted with the project | **Not implemented.** `delete_project` (`services/projects/service.py:190`) soft-deletes the project and cleans up documents + Slack mapping, but never touches `Chatboard`/`ProjectChat` rows — chats survive indefinitely | N/A — backend-only fix | ❌ Open — pre-existing correctness bug, reconfirmed this session |
| CH6 | Scratch chats (outside any project) have no persistent memory and cannot be shared | Not specifically re-verified this session | Not specifically re-verified this session | — Not evaluated this pass |

---

## 3. Projects

| # | Spec requirement | back-end-test reality | Frontend (current) | Status |
|---|---|---|---|---|
| P1 | Project visibility types: **Private** (creator only) vs. **Shared** (explicitly added members) | Only binary `private`/`org` (whole-workspace) visibility exists — no membership concept backs "Shared" at all | Kept binary private/org per explicit user decision (2026-08-29): "keep it private/org only" | ❌ Open — backend-dependent, explicitly deprioritized for now |
| P2 | Project membership is a simple list — any workspace member added can see/use everything in the project, no project-level roles | No such endpoint on either branch. `ORG_PROJECT_MEMBERS_ENDPOINT` family (`/organizations/{id}/projects/{id}/members`) 404s on main and test | Dead `ProjectMembersPanel`/`ProjectTeamPanel` UI removed rather than left broken; no working feature exists | ❌ Open — needs new backend work; no frontend-only fix possible |
| P3 | Shared → Private conversion is never allowed (restrict access by removing members instead) | **Not enforced.** `set_resource_visibility` (`services/organizations/service.py:1465`) lets the owner flip visibility either direction unconditionally — only checks value validity + ownership | UI hides the "Private" option in the share modal once a project is already shared (`project/[id]/page.tsx`) — cosmetic guard only, not real enforcement | ❌ Open — backend gap, frontend workaround is cosmetic |
| P4 | Deleting a project: any Owner/Admin can delete a shared one, only the creator can delete a private one; agents stop running, outputs persist in workspace history | `delete_project` gates on `require_project_editor` — exact owner/admin-vs-creator distinction per spec not independently re-verified this session | Not evaluated this session beyond the chat-cascade angle (see CH5) | — Not evaluated this pass |
| P5 | Removing a member from a shared project: their private chats there become inaccessible to them, their previously-shared chats remain visible to others | Cannot be meaningfully implemented or tested — depends entirely on the project-membership list from P2, which doesn't exist | N/A | — Blocked on P2 |

---

## 4. Connectors

| # | Spec requirement | back-end-test reality | Frontend (current) | Status |
|---|---|---|---|---|
| CO1 | Connections are workspace-wide — never scoped by project or member | `AccountScope` = `personal \| shared` (`services/connectors/schemas.py`); no `organization_wide` flag, no `team_ids`/`team_names`. Re-verified this session: no `project_id`, `visible_to`, or `shared_with` field exists anywhere on `ConnectorAccountOption`/`OrganizationConnectorAccountResponse`/`ConnectorCatalogEntry` — genuinely nothing narrower than the whole org | `connectors.ts`/`org-connectors.ts`/`connectorsUnified.ts` updated to 2-value enum, `organizationWide` field/param removed | ✅ Done this session |
| CO2 | No per-project/per-member credit allocation (deferred to enterprise) | Per-member credit-cap endpoint removed vs. main | Not implemented (nothing to remove) | ✅ Matches |
| CO3 | No connection-level approval workflows — all connections available to all workspace members | Entire personal-request family removed (`POST .../personal-request`, `GET/PATCH .../personal-requests/*`). Re-verified: `list_user_connectors`/member catalog logic has zero remaining Team/approval joins | Self-heal "request access" flow removed from `useConnectorSetupFlow.ts`; 403 now shows raw error, no retry attempt | ✅ Done this session |
| CO4 | Team-scoped connector sharing removed entirely | No `/organizations/{id}/teams/{team_id}/connectors[/{slug}]` routes at all; `services/connectors/router.py` and `service.py` re-read in full this session — zero live Team/TeamConnector/team_approved references anywhere in connectors or persona's connector endpoints (`blocked-connectors`, `connectors`, `connector-hints`) | Team-connector functions (`listOrgTeams`, `listTeamConnectors`, `requestTeamConnector`, `setTeamConnectorStatus`, `removeTeamConnector`) deleted from `org-connectors.ts` | ✅ Done this session |
| CO5 | `GET /organizations/{id}/connectors/{slug}/used-by` — what surface(s) can lose access if a shared connector is removed | **Correction (2026-08-29):** now emits only `surface: "organization"` (`services/organizations/service.py:783-796`) — no `"team"` branch exists at all. Schema (`services/organizations/schemas.py:109-111`) types `surface: str`, unconstrained — nothing enforces this at the type level | `getConnectorUsedBy()`/`ConnectorUsedByEntry` in `org-connectors.ts` — `surface` typed as plain `string`, already tolerant of this; no frontend code branches on a specific surface value today, so no change needed | ✅ No frontend change needed, documented for awareness |
| CO6 | *(Backend hygiene, not frontend-facing)* | `services/connectors/tests/test_automation_permission_contract.py::test_team_revocation_suspends_dependent_automations` calls `inspect.getsource(service.unlink_team)` — **no `unlink_team` function exists** in `service.py` (only `unlink`, for personal connections). Dead leftover from the Team removal; will error on test collection | N/A | ❌ Open — flag to backend team, doesn't block frontend work |
| CO7 | **Spec conflict, found 2026-08-29:** the newer [Connections v1 UX doc](https://docs.superhuman.com/d/Connectors_deAyH5BPP5a/Connectors_surqivP-) (§4, §15) states *"Every workspace user has the same connector experience. There are no owner, admin, or member differences"* / *"No control is role-gated"* — directly contradicting Workspace Model v2's Roles table, which gives Owner/Admin *"Manage Connections"* and denies it to Member | Backend implements the Workspace-Model-v2 version, not the newer doc's: every shared-account mutation (`GET/POST /organizations/{id}/connectors/{slug}/accounts`, `PATCH/DELETE .../connectors/accounts/{id}`) is gated by `require_organization_admin` (`services/organizations/router.py:182-238`) — same on `back-end-test`, this file predates the Team-removal migration | **Live bug, now fixed FE-only (2026-08-29):** the frontend didn't hide/disable the "Shared" option for non-admins at all — a Member could complete the whole Setup flow and only discover the 403 at the very last step. Fixed in `SetupModal.tsx` (locks "Shared" with an "Admins only" badge, blocks reconnecting an existing shared account), `AccountDetailView.tsx` (disables renaming a shared account's label), and `RemoveModal.tsx` (new `blockedReason` prop disables Remove with an inline explanation) — all gated on `canManageShared = currentUserRole === 'admin'`, computed in `ConnectorsExperience.tsx`. This matches *current backend reality*, not the newer doc's stated goal — if backend later drops the admin gate, this becomes dead code to remove (each site comments why) | ✅ FE-only fix shipped · ⚠️ underlying spec conflict (Workspace Model v2 vs. Connections v1 UX doc) needs a product decision on which one backend should implement |
| CO8 | 22-story end-to-end connector flow (Storybook `ConnectorLibraryV1.stories.tsx`) should be reflected in the real Connectors page | **Already a deliberate, complete 1:1 port**, wired to real data (`ConnectorsExperience.tsx` + `ConnectionsView`/`ConnectorDetailView`/`AccountDetailView`/`SetupModal`/`RemoveModal`/`CustomConnectorModal`) — predates this session. Known backend limitations (visibility can't change after creation, shared accounts have no per-tool permission storage, shared OAuth accounts can't reconnect in place, private accounts can't be renamed) are already handled via disabled-state + explanatory tooltip, documented inline as Gaps #1–#14 | N/A | ✅ Already matches — only CO7's role-gating was actually missing |

---

## 5. Personas / Agents

| # | Spec requirement | back-end-test reality | Frontend (current) | Status |
|---|---|---|---|---|
| AG1 | Agents follow the same flattened workspace model as Projects/Chats — no Team dependency | Confirmed clean: `PersonaRepo`/`Persona` models (`services/persona/models.py:28-35`) have `visibility` (`private`/`shared`) + `organization_id` — same shape as `Project`. Grepped `services/persona/` and `services/persona_share/` in full for "team" — the only hits (`router.py:258`, `repository.py:70`) are stale docstring prose ("team-published", "a team or org grant"), not live code; `persona_share` has zero hits at all | No frontend change needed — already consistent | ✅ Already flattened, no team-removal work needed |
| AG2 | Per the Project Chat Model spec, Agents live *inside* Projects — project members create/run agents within that project's context, and project membership governs who can use them | **Contradicted by the schema.** Neither `PersonaRepo` nor `Persona` has a `project_id` (or any project relationship) at all — an agent is owned by a user and optionally shared org-wide (`organization_id`), full stop. There is no data model connecting an agent to a specific project | `personasForTeamContext()` (`lib/api/personas.ts:186-192`) already works around this: "show agents available in this project" is implemented as "show every org-shared agent," not true project-scoping — same shortcut used for `teamChats` (see CH2). The `ProjectAgentsPanel` UI reflects this: a shared project's "agents" tab is really just the whole org's shared-agent catalog, not that project's own | ❌ Open — structural gap, same root cause as P1/P2 (no per-project membership/scoping exists anywhere in the schema yet), but specific to Agents having *no* project relationship at all rather than an over-broad one |
| AG3 | Shared → Private conversion rules for a resource a member owns | Personas share the exact same `set_resource_visibility` function as Projects and Chats (`services/organizations/service.py:1465`, `kind in {"persona","project","chat"}`) — same unenforced Shared→Private reversal described in P3 | No frontend guard exists for persona visibility reversal today (unlike the project page's cosmetic "hide the Private option" fix) | ❌ Open — same root cause as P3, not separately mitigated on the frontend for personas |
| AG4 | Agent sharing ("Super Link" / persona-shares) should work the same regardless of Team's removal | `persona_share` router/schemas re-read in full this session — zero Team/TeamEditor references anywhere; sharing is a flat `persona-shares` resource keyed to user/org, no team indirection | `src/lib/api/persona-shares.ts` already calls the flat endpoints | ✅ Already flattened, no team-removal work needed |

**Deeper role-permissions audit, 2026-08-29** — same treatment as the organizations audit (W6–W13), this time on `services/persona/` and `services/persona_share/`. Opposite failure mode from the org audit: there, Members saw/did too much; here, Members are blocked from too much.

| # | Spec requirement | back-end-test reality | Status |
|---|---|---|---|
| AG5 | Workspace Model v2: all three roles get "full product access... Create Projects, Agents, and run workflows" — no role differentiation on product actions | **Violated, and it's the literal reading of a docstring, not stale prose.** Every mutating persona route (create/edit a version, publish, pause/activate, delete, manage blocked-connectors/connector-hints/documents/knowledge-url, run a test) funnels through `require_owned_repo` (`repo_service.py:262-277`). For an **org-owned** agent (`organization_id` set), that function calls `organizationAdmin()` (`repo_service.py:280-284`, checks `role in (owner, admin)`) — **a plain Member gets a 404 trying to edit, version, publish, or delete a shared agent**, even though the spec says all three roles have equal product access | ❌ Open — over-restrictive; the "any admin edits it" comment on `PersonaRepo.organization_id` (`models.py:31-32`) turned out to be exactly what the code does, not just a stale note |
| AG6 | Internal consistency: whatever governs edit rights on an org-owned agent should govern visibility changes too | **Inconsistent within the backend itself.** The same org-owned agent that an Admin *can* edit/publish/delete per AG5 uses a **different, stricter** gate for its own visibility toggle: `PATCH /persona/{repo_id}/visibility` routes through the generic `set_resource_visibility` (`organizations/service.py:1465-1493`), which requires strict `owner_id == user_id` with **no org-admin override at all**. So an Admin can fully edit an org-owned agent but cannot flip its private/shared switch — only the literal original creator ever can, even after the agent becomes org-owned | ❌ Open — two different authorization models applied to the same resource, not just a spec mismatch but an internal inconsistency |
| AG7 | Creating a new agent — open to any org member, no role gate | `POST /persona` (`router.py:166-201`) is `Depends(get_current_user)` only; `repo_service.create_repo` has no role check | ✅ Matches |
| AG8 | Agent sharing ("Super Link") authorization | Purely ownership-based, no role gating found anywhere. Creating a share requires owning (or, per AG5, being org-admin of) the persona being shared. **Revoking** a share is gated to `share.shared_by_user_id == user_id` (`persona_share/service.py:334-338`) with **no admin override** — once one Admin creates a share on an org-owned agent, no one else (not even another Admin, not the Owner) can revoke it but that specific person | — Not a direct spec violation (persona-shares isn't governed by either doc), but the same "no override even for admins" pattern as AG6 — worth fixing alongside it if AG6 gets addressed |
| AG9 | "Team" terminology fully dead in persona/persona-shares | Re-grepped `schemas.py`/`models.py` this pass (not just router/repository) — only comments/docstrings and one test literal (`tests/test_persona_identity.py:118`) using `visibility="team"`, which bypasses the real enum (`VISIBILITY_VALUES = ("private","shared")`, `organizations/schemas.py:41`) rather than exercising live code | ✅ Confirmed dead, cosmetic only |

---

## 6. Stripe (Billing) / Pins / Highlights / Users

| # | Spec requirement | back-end-test reality | Status |
|---|---|---|---|
| ST1 | Owner-only billing mutations: manage plan/payment, spend cap, checkout, portal | All 9 `stripe/router.py` routes use only `Depends(get_current_user)` — role enforcement happens **inside** `service.py` via `require_billing_owner_or_individual` (`service.py:170-183`), which 403s any org member whose role isn't `owner`. Confirmed on checkout, subscription cancel/resume, topup, topup/charge, and portal | ✅ Matches |
| ST2 | Admin: "Billing — No access at all" | **Violated on the Enterprise-org path.** `GET /billing`'s `is_privileged = role in (owner, admin)` (`service.py:563`) grants Admin the **full** enterprise billing view — invoices, payment method, base fee, overage — via `add_enterprise_billing`. Same category of leak as the org-level W6/W7 findings | ❌ Open — real billing-visibility leak to Admin |
| ST3 | Admin/Member: "Billing — No access" | **Partially violated on the regular (non-enterprise) path.** Only the Owner gets the full `build_subscription_billing` detail (`service.py:591`); Admin/Member get a plan-summary-only `BillingInfo` (`service.py:594-607`) with no invoices/payment method — better than ST2, but still not "no access" (plan_type/status/billing period are visible) | ⚠️ Partial — narrower leak than ST2, still not spec-clean |
| ST4 | "Org tier is dropped, Workspace is the sole boundary" | **Naming mismatch, not a functional bug.** `PlanType.teams` (`services/users/models.py:15`) and `PlanKey.team_*` (`stripe/schemas.py:14-19`) are a **live** Stripe pricing tier — confirmed still wired through checkout/webhook/renewal (~30 references). Purchasing it calls `provision_organization(...)` (`service.py:625`), i.e. it already provisions the Organization/Workspace entity the spec describes — it's just still named "teams"/`team_125` etc. instead of anything workspace-flavored | ⚠️ Cosmetic — functions correctly as the Workspace-tier plan, needs a naming decision, not a functional fix |
| ST5 | **Real bug, found and fixed 2026-08-29:** a brand-new org (no Stripe checkout completed, zero credits ever granted) showed up on `/settings/plans` as if it were actively subscribed to the **cheapest paid tier ($125/mo, "Active" badge, fake next-billing date, cancel-plan link)** — reported by the user as "why is my account on $125/month when I never selected it" | **Backend confirmed correct, not the cause:** `OrganizationResponse.plan_type` is genuinely `None` for a plan-less org (`services/organizations/service.py:261-269`). But `GET /organizations/{id}/plan`'s own `PlanResponse.plan_type` (`schemas.py:219`) defaults to the literal string `"teams"` and is **hardcoded to `PlanType.teams.value`** in the non-enterprise branch of `get_plan()` (`service.py:643`) regardless of whether a subscription exists — this endpoint has no way to represent "no plan" at all, by design, not by bug. Per explicit instruction this session, **no backend change was made** — the fix is entirely frontend | **Frontend bug, root cause:** `settings/(shell)/(org)/plans/page.tsx`'s `currentTierIdx` used `TIERS.findIndex(t => t.credits === totalCredits)` then fell back to index `0` (`{ price: 125, credits: 60_000 }`) whenever nothing matched — which is exactly what happens when `totalCredits` is `0` (never purchased). Fixed by deriving `hasPlan = isEnterprise \|\| totalCredits > 0` (a real credits-ever-granted signal computed purely client-side — `totalCredits = remaining + used` is only ever `0` when a plan was truly never purchased; a real subscriber who fully used their credits would still show `used > 0`) and branching the "Plan" card's render on it: `hasPlan` shows the real price/Active badge/cancel-plan UI as before; otherwise a "No plan selected" badge + "Choose a plan" CTA to `ORG_CHANGE_PLAN_ROUTE`. Also checked: `onboarding/plans/page.tsx` has a similarly-shaped tier-index-defaults-to-0, but that's a legitimate plan-*picker* slider needing a starting position, not a "your current plan" display — and it's part of the already-dead pre-v1.5 onboarding chain, so left untouched | ✅ Fixed (frontend-only, per instruction) |
| PH1 | Pins & Highlights should be personal artifacts, unaffected by the Team removal | **Fully clean.** Both scoped to `user_id` only (`pins/models.py:17`, `highlights/models.py:12`) — no `organization_id`/`team_id` column anywhere. Every route on both is plain `Depends(get_current_user)`, no role gating (correctly, since these are personal) | ✅ No changes needed |
| US1 | Users should carry no leftover Team/org-role fields | **Fully clean.** No Team/org-role field on `User`, `UserResponse`, or `UserAccountResponse` (`users/schemas.py:79-95`). `role_fit`'s `TeamSizeType` enum (`users/models.py:41-44`) is referenced only within the users service itself (`users/service.py:10,436`) — not leaked into pins/highlights/stripe | ✅ No changes needed |

Net: pins, highlights, and users needed no changes at all. Stripe's mutation endpoints are correctly Owner-gated, but its two billing-read paths (ST2/ST3) leak billing visibility to Admin — ST2 especially, where Admin gets the *entire* enterprise billing picture the spec explicitly denies them. ST4 is a rename-only item, already functionally correct.

---

## 7. Workspace & Organization (catch-all)

| # | Spec requirement | back-end-test reality | Frontend (current) | Status |
|---|---|---|---|---|
| W1 | Workspace is the sole commercial/org boundary; "org tier" dropped | Still literally `Organization` everywhere — no rename (`services/organizations/*`) | "Workspace" used in user-facing copy only, by deliberate decision — no code identifiers renamed | ✅ Cosmetic-only, confirmed intentional |
| W2 | Three fixed roles: Owner / Admin / Member, per-workspace | **Correction (2026-08-29):** the actual `OrganizationRole` enum (`services/organizations/models.py:23-34`) has **4** values — `owner`, `admin`, `member`, and `service` (a machine/anonymous principal for someone who reached the product through a surface, e.g. Slack, without ever signing up — not a real permission tier). `resolve_role` (`roles.py:75-83`) treats anything that isn't `owner`/`admin` as a plain `Member`, so `service` inherits Member-level access rather than having its own tier — the spec's "three fixed roles" claim holds in practice for authorization purposes | `OrgRole` type matches the 3 user-facing roles exactly (`src/lib/api/organization.ts`) — `service` is never surfaced to the frontend, no change needed | ✅ Aligned (3 user-facing roles) · note the 4th internal `service` value for future readers |
| W3 | Teams entity removed entirely | Zero `/teams` routes anywhere; confirmed via full router/schema read | Dead Teams UI removed (nav link, `ProjectMembersPanel`/`ProjectTeamPanel`), team-connector/team-invite functions deleted from `org-connectors.ts`/`teams.ts` | ✅ Done this session |
| W4 | Invites are workspace-level, not team-scoped | `POST/DELETE /organizations/{id}/invites` — flat, no `team_id` | `inviteMembers()` already called the flat path pre-migration | ✅ Matches |
| W5 | Invite preview/accept | Router renamed `/team-invite` → `/org-invite` (`services/organizations/router.py:489`); accept returns `OrganizationResponse` not `TeamResponse` | `ORG_INVITE_PREVIEW_ENDPOINT`/`ORG_INVITE_ACCEPT_ENDPOINT` added and wired in `teams.ts`; function/type names (`getTeamInviteOnboarding`, `TeamInviteOnboarding`) deliberately left as-is — renaming them touches a whole `onboarding/team/[inviteId]/*` route tree, out of scope for "app copy only" | ✅ Endpoint fixed · ⚠️ naming still says "Team" internally (cosmetic, low priority) |

**Deeper role-permissions audit, 2026-08-29** — every organization endpoint checked against the Workspace Model v2 Roles & Permissions matrix (not just spot-checked). Dependency chain confirmed sound: `require_organization_admin` checks `isinstance(ctx.role, Admin)`, and since `Owner(Admin)` (`roles.py:56`), every admin-gate also passes Owners as intended.

| # | Spec requirement | back-end-test reality | Status |
|---|---|---|---|
| W6 | Member: "Billing — No access" | **Violated.** `GET /organizations/{id}/plan` (`router.py:149-154`) only requires `get_organization_context` — any Member passes. `service.get_plan` (`service.py:576-653`) returns plan/credits/pool-cap **and every member's `usage_total`** (588-598, 632-640) to whoever calls it | ❌ Open — Member gets full org billing + everyone's usage, not just their own |
| W7 | Member: "View own usage and project-level usage... " only, not org-wide | **Violated.** `GET /members`, `/members/admins`, `/members/regular` (`router.py:327-363`) only require `get_organization_context`; `member_to_response` (`service.py:455-472, 525-563`) embeds every member's `usage_total` regardless of caller's role | ❌ Open — same root leak as W6, via the member-list endpoints |
| W8 | "Only Owners can change roles" — Admin cannot promote to Admin or Owner | **Violated (partially).** `PATCH /members/{id}/role` is `require_organization_admin` — an Admin can call it. `set_member_role` (`service.py:864-881`) only blocks setting `role=owner` (871-875) — **an Admin CAN promote a Member to Admin**, which the spec reserves to Owner | ❌ Open — role-elevation to Admin isn't Owner-gated |
| W9 | Admin: "Invite and remove Members" — not Admins | **Violated.** `POST /invites` is `require_organization_admin`; `create_invite` (`service.py:908-970`) only blocks `role=owner` invites (927-931) — **an Admin can send an admin-role invite** | ❌ Open — Admin can invite peers at Admin level, spec says Members only |
| W10 | Admin: removes Members, implicitly not other Admins (spec doesn't grant Admin authority over Admin) | **Looser than spec.** `DELETE /members/{id}` is `require_organization_admin`; `remove_member` (`service.py:884-897`) only blocks removing the Owner (888-892) — an Admin can remove another Admin | ❌ Open — Admin has removal power over peers, not just Members |
| W11 | Owner-only: "Configure workspace settings (name, logo, timezone)" — Admin excluded | **Violated.** Both `PATCH /organizations/{id}` (name/slug/logo/tags, `router.py:117`) and `PATCH /organizations/{id}/settings` (`router.py:143`) use `require_organization_admin` — Admin can edit workspace settings the spec reserves to Owner | ❌ Open |
| W12 | "Delete workspace (Primary Admin only)" — an internal flag on one Owner | **No such flag exists.** `Organization`/`OrganizationMember` models have no primary/flag column at all. Instead there's a structurally different mechanism: a partial unique index enforces exactly one Owner per org at all times (`ix_OrganizationMember_single_owner`, `models.py:165-169`), ownership only moves via `transfer_ownership` (demotes caller, promotes target), and `DELETE /organizations/{id}` is gated on `require_organization_owner`. Functionally similar (only the one Owner can delete) but implemented as "the sole Owner" rather than a "primary" designation distinguishable from co-Owners — moot today since multi-Owner isn't actually possible, but diverges from the spec's stated design if multi-Owner is ever added | ⚠️ Structural difference, not currently exploitable (single-Owner invariant makes it moot) |
| W13 | Confirmed matches, no gaps | `POST /transfer-owner` (Owner-only) · `PATCH /plan/pool-cap` (Owner-only) · `GET /plan/usage`, `/pool-status`, `/plan/enterprise-usage`, connector-account endpoints, Slack endpoints (all Admin-gated, matching "Owner/Admin manage Connections, view usage") · `GET /audit` (correctly scopes non-admins to `actor_user_id=ctx.user_id` only, `service.py:1452`) · plain reads (`GET /organizations/{id}`, `/settings`) open to any member, consistent with spec not restricting reads | ✅ No action needed |

Net: **W6/W7 (usage/billing data leaking to Members) is the most serious** — it's a real information-disclosure bug, same category of severity as the chat-privacy issue (CH1/CH2), not just a migration nicety. W8–W11 are authorization gaps that let Admin exceed its documented authority (self-promotion to peer-Admin, peer-removal, workspace settings) rather than anything exposing member data outward. None of this is frontend-fixable — these are all backend authorization/query gaps.

| W14 | **Proposed change, raised 2026-08-29 in conversation — not yet a formal spec doc:** collapse the 3 user-facing roles down to 2 — "Admin" (full access including billing, absorbing today's Owner-only powers) and "Member" — so any Admin has billing/ownership-level access | **Not achievable frontend-only, confirmed.** `PATCH /organizations/{id}/plan/pool-cap`, `POST /transfer-owner`, and `DELETE /organizations/{id}` are hard-gated to `require_organization_owner`, not `require_organization_admin` — an Admin (non-Owner) would still 403 on all three. `set_member_role`/`create_invite` explicitly block ever assigning `role=owner` to anyone. A DB-level unique index (`ix_OrganizationMember_single_owner`, `models.py:165-169`) enforces exactly one Owner per org at all times. None of this can be worked around client-side | Not started. A cosmetic-only stopgap was discussed (always display "Admin" instead of "Owner" — the frontend already partially normalizes this way in `OrgMember.role`, `organization.ts`) but explicitly **not recommended**: it would reintroduce the same "UI promises an action the backend won't allow" bug pattern just fixed for connectors (CO7), except for billing/ownership-transfer/delete-workspace instead | 🔒 Held — needs the backend gates above relaxed (`require_organization_owner` → `require_organization_admin` on the three endpoints, plus revisiting the owner-only role-assignment blocks) before any frontend change is meaningful |

---

## 8. Remaining services — Brain, Automations, Memory, LLM, Internal/Sandbox, Doc Design, Slack, Templates, Docx

Completes the sweep of every service directory in `back-end-test`. Two parallel audits, same method as the earlier sections: full router read, every `Depends(...)` traced to its real check, case-insensitive "team" grep across router/service/repository/models/schemas, cross-referenced against Workspace Model v2's "all three roles get equal product access, differ only on billing/people-admin."

| # | Service | back-end-test reality | Status |
|---|---|---|---|
| RS1 | **Brain** | Fully clean. All 8 routes (`router.py:54-276`) use only `get_current_user`; every chat lookup re-checks `chat.user_id == user["sub"]` before acting (e.g. lines 142,173,230,243,256,269). `repository.py:39-53` scopes list/get by `user_id`. No role gating, no Team references anywhere | ✅ No changes needed |
| RS2 | **Automations** | Fully clean. `GET/PATCH/DELETE/POST run` all use `get_current_user` + `ownedOr404(userId=...)` (`router.py:46-50`) — no admin/owner check anywhere, matching "equal product access." The webhook (`POST /integrations/automations/{id}/run`) is HMAC-signature-verified over the raw body with a 300s replay window (`connectors/webhook_auth.py:57-86`), 401s before any existence leak — not anonymously callable. Automations are per-user owned (no `organization_id` column at all, `models.py:25`) — spec only requires equal create/run rights, says nothing about cross-member workflow sharing, so this isn't a gap. All "team" hits are Slack's own external `team_id`, unrelated to the removed entity | ✅ No changes needed |
| RS3 | **Memory** | Fully clean. The only public route (`POST /memory/user`) writes solely to the caller's own index. Organization-scoped memory is never reachable via a client-supplied org id — every call site derives it server-side from the caller's own membership row (`memory/scope.py:56-60`), and `scopeClauses`/`scopeValues` (`repository.py:75-121`) enforce that scope on every path. `ProjectIndex`'s "Team-shared" docstring (`models.py:181`) is dead comment only — the live FK is `project_id`, correctly scoped | ✅ No changes needed |
| RS4 | **LLM** | Fully clean. All routes (`models` list/all, block, test) use `get_current_user`, scoped by `user_id`. Org instructions resolve via the caller's own membership, never client-controllable. No Team references, no role gating | ✅ No changes needed |
| RS5 | **Internal/Sandbox** (`services/skills/connected_code`) | Correctly isolated — a genuine positive finding, not just "clean." All ~30 routes authenticate via a raw capability token (not a user JWT), minted server-side per run, Redis-backed with a 1800s TTL, bound at mint time to one `user_id`/`chat_id`/file-set/connector allow-list (`context.py:92-170`). No normal end-user Auth0 token can reach these endpoints. `team_id` references throughout are Slack's own workspace-id concept riding on the token, correctly non-forgeable — no internal-Team leftover | ✅ Correctly isolated |
| RS6 | **Doc Design** | Clean. Personal endpoints scoped to the caller. Org-scoped read (`GET /organizations/{id}/doc-design`) is open to any member — matches spec. Org-scoped mutations (upload/activate/deactivate/delete) require `require_organization_admin` — judged a defensible reading (org-wide design sits closer to "manage Connections"/branding-as-setting than pure product content), not a clear violation. No Team references anywhere | ✅ No changes needed (judgment call, not a gap) |
| RS7 | **Slack — install/oauth/status/link/webhooks** | Correct throughout. Install is Admin+-gated (`require_admin_org`) and re-checked server-side on the OAuth callback via signed state — defense in depth. `oauth/callback` and `pipedream/callback` are legitimately unauthenticated public landings with signature/state verification. Personal `status`/`link`/`unlink` are correctly open to any member, no role gate. `events`/`interactivity` are genuine Slack-signature-verified webhooks, no user auth expected | ✅ No changes needed |
| RS8 | **Slack — org channel-binding GET is over-gated** | The **read-only** lookup (`GET /organizations/{id}/slack/projects/{project_id}/channel`, and the installation-status GET) requires `require_organization_admin` — but this is a product-usage read ("is this project's Slack channel bound"), not a Connections-management mutation. Spec says product access should be equal across all three roles; the *mutations* (create/rename/delete channel, remove installation) are correctly Admin+ as Connections management, but gating the plain read the same way is stricter than spec requires | ❌ Open — read-only endpoint over-gated relative to spec |
| RS9 | **Slack — genuine Team leftover found** | `SlackAgentAccount.team_id` (`services/slack/rally.py:68-75`) is a real dead field, literally commented *"The org Team a shared account belongs to — not the Slack workspace"* — always `None` since `accountCard()` never sets it and `permissions.Account.scope` only supports `"personal"`/`"shared"` (`connectors/permissions.py:55`). Everywhere else in Slack code, `team_id`/`"team"` correctly refers to Slack's own workspace vocabulary, not this — distinguished carefully, not a false positive | ❌ Open — flag to backend team, dead field cleanup, same category as CO6 |
| RS10 | **Templates** | Fully clean. User + optionally org-scoped (`models.py:13-16`); every route just requires `get_current_user`; org-member visibility has no admin distinction (`service.readable`, `service.py:108-125`), matching equal product access. Delete is creator-only — an ownership rule, not a role hierarchy | ✅ No changes needed |
| RS11 | **Docx** (`services/skills/docx`) | Fully clean. All 5 routes use `get_current_user`, keyed by `user["sub"]` — called from the end-user's own authenticated browser session, not the sandbox. `include_in_schema=False` is just to keep it off public API docs, not a different auth model | ✅ No changes needed |

**This completes the full sweep of every `back-end-test` service directory against Workspace Model v2 / Project Chat Model.** Net across RS1–RS11: overwhelmingly clean — 9 of 11 rows need no action. The only two real findings (RS8, RS9) are both in Slack, both minor relative to the severity found elsewhere in this doc (CH1/CH2, W6/W7, ST2) — one over-gated read, one dead leftover field.

---

## Notes for next pass

- CH6 (scratch chat memory/sharing) and P4 (project deletion authorization rules) — not checked this session.
- W5's naming cleanup (`getTeamInviteOnboarding` → something workspace-flavored) is cosmetic and touches a large route tree (`onboarding/team/[inviteId]/*`) — only worth doing as a deliberate, scoped rename pass, not incidentally.
- O2/O3's soft-fail-then-advance behavior was flagged but not fixed — revisit if silent data loss during onboarding becomes a real user complaint.
- CH1/CH2 (chat privacy leak) is the single highest-priority item here — it's a live authorization bug independent of the Team→Workspace migration, not something that can wait for a broader project-membership rebuild (P2).
- CO6 is a backend test-suite bug, not a frontend concern — noted for completeness/handoff only.
- CO7's underlying spec conflict (Workspace Model v2 vs. Connections v1 UX doc, on whether connector management is role-gated at all) needs a product decision — the frontend fix matches current backend behavior, not necessarily the final intended one.
- AG2 (Agents have no project relationship in the schema at all) is worth surfacing alongside P2 (project membership) if/when that work is scoped — they're likely the same backend change (a real Project↔Agent/Project↔Member join table would resolve both).
- **Clarification (2026-08-29): "no Member restrictions" is true for connectors specifically, not generally.** Workspace Model v2 explicitly restricts Members on billing, people-management, Connections, and workspace settings — that's the spec's intended design, not a bug (and the backend correctly enforces it for Connections/settings, see W13). It's only the newer Connections v1 UX doc (CO7) that contradicts this, and only for the connector surface specifically. Don't read CO7 as evidence that Member restrictions are wrong everywhere — W6–W11 above are the opposite problem (Members seeing too much, Admins able to do too much), not Members being wrongly restricted.
- W14 (2-role Admin/Member proposal) is a live conversation topic, not a sourced spec requirement yet — revisit its status once/if it's formalized in a doc, and re-check whether the three Owner-gated endpoints it depends on have changed.
- AG5/AG6 (Members blocked from editing org-owned agents; visibility toggle inconsistent with the rest of the edit gate) are the mirror image of the W6–W11 org-endpoint findings — there Members/Admins had too much access, here Members have too little and the backend contradicts itself on what "org-owned" actually grants an Admin. Both AG5 and AG6 are backend-only fixes; nothing to do on the frontend until `require_owned_repo`'s `organizationAdmin()` gate and `set_resource_visibility`'s strict-owner check are reconciled.
- ST2 (Admin gets full enterprise billing view) is worth prioritizing alongside W6/W7 — same information-disclosure category, same fix shape (narrow a role check). ST4's "teams" plan-tier naming is low-priority cleanup, not a bug — flag it if/when a broader terminology pass happens, don't chase it standalone.
- **Full backend sweep complete as of 2026-08-29** (Section 8) — every service directory in `back-end-test` has now been audited at least once against the specs. Only RS8 (Slack channel-binding read over-gated) and RS9 (dead `SlackAgentAccount.team_id` field) remain open there; everything else (Brain, Automations, Memory, LLM, Internal/Sandbox, Doc Design, Templates, Docx) is clean. RS9 can be batched with CO6 (also a dead-field/test cleanup item) as a single small backend hygiene PR.
