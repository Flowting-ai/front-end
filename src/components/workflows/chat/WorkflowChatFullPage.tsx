/**
 * Backward-compatibility shim.
 * All logic lives in WorkflowChat.tsx — import from there for new code.
 */
export { WorkflowChat as WorkflowChatFullPage } from "./WorkflowChat";
export type { WorkflowChatProps as WorkflowChatFullPageProps } from "./WorkflowChat";
