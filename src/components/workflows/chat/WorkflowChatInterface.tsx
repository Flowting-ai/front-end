/**
 * Backward-compatibility shim.
 * All logic lives in WorkflowChat.tsx — import from there for new code.
 */
export { WorkflowChat as WorkflowChatInterface } from "./WorkflowChat";
export type { WorkflowChatProps as WorkflowChatInterfaceProps } from "./WorkflowChat";
