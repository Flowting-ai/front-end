/**
 * Backward-compatibility shim.
 * All chat code lives in chat/WorkflowChat.tsx — import from there for new code.
 */
export { WorkflowChat } from "./chat/WorkflowChat";
export type { WorkflowChatProps } from "./chat/WorkflowChat";
