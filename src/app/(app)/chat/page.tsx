// Chat page — full implementation arrives Day 5 (streaming core)
// Renders inside AppLayout (sidebar + topbar already provided by group layout)

export default function ChatPage() {
  return (
    <div
      style={{
        flex: 1,
        display: "flex",
        flexDirection: "column",
        alignItems: "center",
        justifyContent: "center",
        height: "100%",
        minHeight: "400px",
      }}
    />
  );
}
