# Front-End ↔ Back-End Alignment — Execution Map

**Scope:** the live `front-end/` codebase only — the product people actually use today (Org → Team → Project, actively maintained). This is **not** `docs/EXECUTION_MAP.md`, which governs a separate, mostly-unstarted rewrite into `front-end-new/` on Next.js 16 + the Kaya Design System and has no relationship to the gaps below.

**Origin:** this map executes the front-end side of a back-end/front-end compliance audit run against the Superhuman Docs "Workspace Model v2," "Project Chat Model," and "Billing & Credits" pages. Full audit: [Workspace v2 Compliance Ledger](https://claude.ai/code/artifact/333ccef9-264a-49ff-a290-d9ae8d65022a). That audit's Part II ("back-end → front-end transition") is the backlog this map turns into sequenced work — the question here is narrower than spec compliance: **has the front-end caught up to what the back-end already did**, regardless of whether the back-end itself is fully spec-compliant yet.

**Method note — verify before trusting prior docs.** Building this map, two claims in `docs/organizations/role-guardrails.md` (dated 2026-06-18) turned out to already be fixed in current code (§1 below). The org/team surface changes fast; treat any status claim — including this map's, past today — as a hypothesis to spot-check against source, not a fact to build on unread.

---

## 1. Already resolved — do not rework

Two items the 2026-06-18 audit trail flagged as gaps are confirmed fixed in current code. Skip these; re-litigating them would be wasted effort.

- **Workspace credit banner is wired and live.** `useWorkspaceCreditNotice()` (`src/hooks/use-workspace-credit-notice.ts:22-24`) reads the real `plan.poolStatus` field and drives `<InlineCreditNotice>`. It's mounted in 8 real surfaces: `ChatInterface.tsx`, `chat/page.tsx`, `project/[id]/page.tsx`, `project/[id]/chat/[chatId]/page.tsx`, `PersonaChatInterface.tsx`, `agent/configure/layout.tsx`, `templates/Brain/index.tsx`. The old claim ("banner reads a dead `tokenStatus` prop", "not rendered in any layout") no longer holds.
- **Owner is not excluded from admin checks.** The `const isAdmin = currentUserRole === 'admin'` pattern in `members/page.tsx:1782`, `activity/page.tsx:114`, `teams/page.tsx:284`, `teams/[teamId]/page.tsx:587` looks suspicious in isolation, but `currentUserRole` itself already folds Owner into `'admin'` before it reaches those comparisons (`org-context.tsx:217-221`: `resolvedRole === 'owner' || resolvedRole === 'admin' ? 'admin' : ...`). No fix needed here.

---

## 2. Progress tracker

| Thread | Focus | Status |
|---|---|---|
| A | Team → Organization flattening | 🟡 A1-A3b done (`d422752`, `c942b12`, `cce51a2`, `2c8c1dc`); A3c/A4/A5/A7 remain — see sub-threads |
| B | Orphaned endpoint triage (credit overflow) | ✅ Done — committed `c36828b` |
| C | Doc hygiene | ⬜ Not started |

---

## 3. Thread A — Team → Organization flattening

The back-end already did this (five migrations, ending in `flatten_teams_into_organizations` — `Team`/`TeamEditor`/`TeamViewer`/`TeamInvite`/`PersonaTeam` tables dropped, everything reparented onto `Organization`). The front-end has an existing, detailed plan for this at `docs/organizations/team-to-org-flattening-migration-plan.md` — this thread absorbs it.

**Governing rule for all of Thread A (set by the user 2026-08-20): only wire up what the back-end already exposes. Never invent replacement UX for a capability that has no back-end route.** Where a team-scoped feature has no org-scoped or project-scoped equivalent anywhere in the back-end, the correct action is deletion with no replacement — not a redesigned version of it. Flag the resulting capability gap; don't fill it.

**services/organizations/router.py was read in full this session — this is the authoritative, current route list** (openapi.yaml is still stale, don't use it):
`GET/POST /`, `GET/PATCH/DELETE /{id}`, `POST /{id}/transfer-owner`, `GET /{id}/settings` (+PATCH), `GET /{id}/plan` (+`/pool-cap` PATCH, Enterprise-only, +`/usage` → `by_member`, +`/enterprise-usage`), `GET /{id}/pool-status`, `GET /{id}/members` (+`/admins`, `/regular`, `/{member_id}/role` PATCH, `/{member_id}` DELETE) — **no member-cap endpoint exists**, `GET/POST /{id}/connectors` (+`/{slug}` PATCH/DELETE, `/catalog`, `/{slug}/used-by`, `/{slug}/accounts` GET/POST, `/accounts/{account_id}` PATCH/DELETE), `GET/POST/PATCH/DELETE /{id}/connections/{slug}` (+`/link`), `GET/POST/DELETE /{id}/projects/{project_id}/members(/{member_id})` — **flat, no team_id in path**, `POST/DELETE /{id}/invites(/{invite_id})` — **one flat org-level invite, `InviteRequest{emails, role, project_id}`, no team grant fields at all**, `GET /{id}/audit`. Separate routers: `/chat-shares/*` (create/list/shared-with-me/{id}/{id}/fork/{id} DELETE — no org or team scoping in the path at all) and `/team-invite/{invite_id}` (+`/accept`) — **URL prefix is a naming leftover; the actual response (`InvitePreview`) is fully org/project-shaped, zero team fields.**

### Confirmed dead — no back-end route, delete with no replacement (per the governing rule)
- **All of `lib/api/teams.ts` except invite preview/accept**: `fetchTeams/getTeam/createTeam/updateTeam/archiveTeam/deleteTeam` (Team CRUD), `listTeamEditors/addTeamEditor/removeTeamEditor` (TeamEditor grants), `listTeamConnectors/requestTeamConnector/setTeamConnectorStatus`, `listTeamConnections/createTeamConnectionAccount/attachSharedAccount/unlinkTeamConnection`, `listTeamPersonaShares`/`fetchPersonaOwnerMap`. Checked against a real, separate `services/persona_share` router (create/list/received/sent/dashboard/{id}/{id}/accept/{id} DELETE) — it's a different feature (individual share links), not a drop-in for the team-wide ownership map `fetchPersonaOwnerMap` provided. No replacement exists for "who owns this persona across my team" — capability gap, flag it, don't invent one.
- **The entire per-member credit-cap system**: `setMemberCap()`/`ORG_MEMBER_CAP_ENDPOINT` (`organization.ts:399`), `CreditCapRow`, `AssignCreditsModal`, and the "Credit caps section" in `members/page.tsx` (~line 1705 onward: header, `cappedMembers`/`totalCapped`/`uncappedCount` stats, per-row assign UI, the invite flow's `creditCap` param, the optimistic `setMemberCap` call at line 2161-2169). The router has no member-cap endpoint at all.
- `getOrgPlanUsage()`'s assumed `by_team: TeamBurnResponse[]` shape (`organization.ts:103-114, 211-222`) — real response is `by_member: MemberBurn[]` (`organization_id, by_member: [{user_id, name, email, credits_used}]`). Currently silently broken (`.catch(console.error)` swallows the shape mismatch) — the Analytics page's usage-by-team section (`analytics/page.tsx:679-684, 722`) has been silently empty, not actually crashing.
- `memberResponseSchema`'s `credit_cap`, `credit_used`, `team_id`, `team_name` (`organization.ts:60-74`) — real `MemberResponse` is only `user_id, name, email, role, usage_total, invite_status, invite_id, is_pending_invite`. These currently silently default (null/0/empty) rather than error.
- `SlackChannelMapping.teamId`/`teamName` (`types/teams.ts`) — **explicitly out of this thread.** The Slack integration is its own back-end service (42 files reference `team_id` there) that neither the original migration plan nor this session's audit covers. Don't touch without a dedicated look at `services/slack` first.

### Confirmed needs new wiring — back-end already has it, front-end has zero consumers
- Org-level connector request/status (`list_org_connectors`/`request_org_connector`/`set_org_connector_status`/`remove_org_connector`, `/{id}/connectors`) and org-level connections (`list_org_connections`/`link_org_connection`/`update_org_connection`/`unlink_org_connection`, `/{id}/connections/{slug}`) — confirmed zero front-end call sites anywhere in `src`. These are the correct replacement targets for the deleted team-scoped connector/connection functions (same job, existing route) — not new logic, just wiring up what's already built.
- Org-level project members (`/{id}/projects/{project_id}/members`) already has a close analog in `teams.ts`'s `listProjectMembers`/`addProjectMember`/`removeProjectMember` — same rewiring pattern, just drop the `teamId` param.
- Org-level invite (`/{id}/invites`, flat `{emails, role, project_id}`) replaces `inviteTeamMembers` — drop `grantTeamEditor`/`grantTeamViewer`/`creditCap`, none of which the real `InviteRequest` schema accepts.

### Sub-threads (in dependency order)
1. **A1 — Data-layer correctness fixes. ✅ Done — committed `d422752`.** Fixed `memberResponseSchema`/`normalizeMember` to stop declaring `credit_cap`/`credit_used`/`team_id`/`team_name` (backend never sends them) — kept as a schema-only fix, `OrgMember`'s output shape untouched so zero consumer files needed changing (that's A2's job). Fixed the real, previously-silent `by_team`→`by_member` mismatch in `getOrgPlanUsage`/`PlanUsageResponse`/`types/teams.ts` (`TeamBurn`→`MemberBurn`), which had left the Analytics page's second usage list silently empty. `tsc --noEmit` clean; React Doctor confirms zero new issues (`--scope changed --base 4bbd108`).
2. **A2 — Remove the dead credit-cap system. ✅ Done — committed `c942b12`.** Deleted `CreditCapRow`, `AssignCreditsModal`, `setMemberCap`/`ORG_MEMBER_CAP_ENDPOINT`; removed the "Credit caps section" from `members/page.tsx` and the credit-cap input/pool-remaining warning from `InviteModal`/`AppInviteModal`; removed `OrgMember.allocationUsed`/`creditCap` (write-only once the above was gone). Analytics page's `MemberCapsTable` (renamed `MemberUsageTable`) kept its still-real Member/Credits-used columns and search/filter — only the always-empty Cap/Usage columns were removed, with the matching skeleton grid updated. Deliberately left untouched: `teams.ts`'s `inviteTeamMembers`/`getTeamInviteOnboarding` creditCap handling and the onboarding invite-confirm page — different code path, covered by A4. `tsc --noEmit` clean; eslint shows only 4 pre-existing warnings (verified against the pre-session commit); React Doctor flagged 2 pre-existing try/finally-vs-React-Compiler issues whose span happens to include edited lines — confirmed identical in `868c85e` (before this session), not introduced by this diff.
3. **A3 — Rewire invites + project-membership. ✅ Done — committed `cce51a2`.** Turned out to be two confirmed-broken production bugs, not just cleanup: (a) org invites were completely non-functional — `inviteTeamMembers()` required an existing team, but Team can never exist anymore (table dropped), so both `members/page.tsx`'s invite flow and onboarding's invite step always failed; (b) invite revocation silently used the wrong endpoint (`OrgMember` never actually carries a team id, so it always fell through to `removeMember` instead of the real invite-delete route). Rewired to the actual flat endpoints — `inviteMembers(orgId, emails, role?, projectId?)` → `POST /organizations/{id}/invites`, `revokeInvite(orgId, inviteId)` → `DELETE /organizations/{id}/invites/{id}`, `listProjectMembers`/`addProjectMember`/`removeProjectMember` → `/organizations/{id}/projects/{project_id}/members` (no team_id in the path at all). Touched `teams.ts`, `organization.ts`, `config.ts`, `types/teams.ts`, `ProjectMembersPanel`, `members/page.tsx`, `lib/team-access.ts`, the admin team settings page, and onboarding's invite step. `tsc`/eslint/React Doctor all verified clean of new issues.

   **Deliberately deferred, not part of A3:** Team CRUD, TeamEditor grants, team connector/connection functions, team persona shares are all still exported from `teams.ts` and still call dead team-scoped routes — but they're now provably unreachable in practice, since org-context's team list can never be non-empty (the backend's Team table is gone, so nothing can ever populate it). Rewiring or deleting these forces touching `org-context.tsx`'s team-list surface, which cascades into ~15+ more files: `LeftSidebar`, `TeamSwitcher*`, the two Team settings pages (CRUD + roster + editor grants + archive/delete), the 3000-line `connectors/page.tsx` (4 of 5 tabs are team-scoped), `TeamAgentsTab`, and `members/page.tsx`'s per-team role-management modal. That's real, valuable follow-up work, but it's UI-deletion-and-redesign scale (like A2, but several times larger) — not bundled into this fix. See the new sub-thread breakdown below (A3b) for how to sequence it.
4. **A3b — Delete Team CRUD (functions + pages + nav/search/welcome wiring). ✅ Done — committed `2c8c1dc`.** Deleted `fetchTeams/getTeam/createTeam/updateTeam/archiveTeam/deleteTeam` + the per-org cache from `teams.ts`, `ORG_TEAMS_ENDPOINT`/`ORG_TEAM_ENDPOINT` from `config.ts`; deleted both admin Team settings pages (list + 1300-line roster/editor-grants/connectors/archive-delete detail), the live **non-admin** `app/(app)/teams/[teamId]/page.tsx` (confirmed not a duplicate, but its core `getTeam` load call had no route either, and nothing can ever link to a real `teamId`), and the two legacy `/org/teams/*` redirect stubs; removed the "Teams" settings-nav item, the global-search "Teams" result, and the welcome page's "Create your first team" card (these actively promoted a dead destination, not just sat unused). Fixed `org-context.tsx`'s now-impossible `fetchTeams` effect — `teams` was already initialized to `[]` and stays there forever, `teamsLoading` collapses to a constant `false`, `refreshTeams` becomes a no-op. `tsc --noEmit` clean (after clearing a stale `.next` cache); eslint/React Doctor: 2 genuinely new issues found and fixed (unused import, unused setter), everything else confirmed identical to `868c85e`.

   **Deliberately left in place** (same "unreachable but not directly linked, flag as debt" policy): `LeftSidebar.tsx`'s `onCreateTeam`/`resolveTeamAction`/`TeamsSidebarContentProps` and `members/page.tsx`'s `goToTeamSettings` still push to `ORG_TEAMS_ROUTE`/`ORG_TEAM_ROUTE`/`TEAM_ROUTE`, which now 404 — fixing this properly means the full TeamSwitcher teardown (see A3c below), not a quick patch. `listTeamEditors`/`addTeamEditor`/`removeTeamEditor` and the team connector/connection/persona-share functions are still unchanged in `teams.ts` for the same reason.
5. **A3c — TeamEditor grants, `connectors/page.tsx` flatten, persona-shares, TeamSwitcher teardown.** Not started. Confirmed via full-file mapping this session:
   - **`members/page.tsx`'s `handleManageRole`** (~1856-1954) implements a whole per-team editor/member role-diffing system (`ManageRoleModal`, `desiredTeamRoles: Record<string, TeamRoleValue>`) — currently unreachable (no teams to diff against) but built around `addTeamEditor`/`removeTeamEditor`/`removeProjectMember`. Needs simplifying to a plain org-role (admin/member) change once TeamEditor is actually deleted.
   - **`ProjectTeamPanel`** — its "Team members" block (importing `listTeamEditors`) is fully dead; delete it, keep the `ProjectMembersPanel` embedding. **`ProjectMembersPanel`** additionally uses `listTeamEditors` for *eligibility filtering* (excluding existing team editors from the add-member picker) — this is a real behavior change, not a param drop, once removed (the "Team editors already have access and won't appear in this list" subtitle becomes false too). **`lib/team-access.ts`**'s `fetchTeamAccessSnapshot` loses its entire `editorsByTeamId`/"isTeamOwner" side — its only remaining consumer was the settings team-roster page already deleted in A3b, so this file may now be entirely dead — check before touching.
   - **`LeftSidebar.tsx`'s TeamSwitcher UI** (`onCreateTeam`, `resolveTeamAction`, `TeamsSidebarContentProps`) and `members/page.tsx`'s `goToTeamSettings` — currently push to routes A3b deleted the destinations of. Needs removing/redirecting so nothing dead-links.
   - **`app/(app)/connectors/page.tsx` (3000+ lines)** — NOT just the "team-access" tab. `listTeamConnectors`/`setTeamConnectorStatus` also back the **Requests** tab's team-request approval; `listTeamConnections`/`attachSharedAccount`/`unlinkTeamConnection`/`createTeamConnectionAccount` back the **shared-accounts** tab's per-team toggle and the non-admin **member-browse** view. All of it calls team-scoped routes that don't exist — this needs a genuine flatten-to-org-scoped rewrite across 4 of 5 view modes (`MainTab` union, `ADMIN_TABS`, `TeamAccessTab`, `PermissionsTab`, `AccountDetailView`, `MemberBrowseView`), using the new org-level connector/connection functions from "needs new wiring" above. This is its own large, standalone effort — don't bundle with the rest of A3b.
   - **`TeamAgentsTab.tsx`** — entirely built on `listTeamPersonaShares`; cleanly removable as a whole tab (`agents/page.tsx:60,1571-1574`, gated by a simple `activeTab === 'team-agents'` conditional, not interleaved with other logic).
   - **`fetchPersonaOwnerMap`** (`chat-personas.ts`, `agents/page.tsx`) — removing with no replacement degrades gracefully but changes real behavior: non-admin viewers always auto-copy team personas instead of chatting directly as the true owner; admins get treated as owner of every team persona; the "Shared by" footer can't resolve a name. Document this as an accepted capability gap, not a bug to chase.
6. **A4 — Onboarding invite flow.** Rewrite `getTeamInviteOnboarding` (`teams.ts:309-343`) and its `InviteOnboardingResponse`/`TeamInviteOnboarding` types to match the real `InvitePreview`/`InviteProjectPreview`/`InviteUserPreview` schemas exactly (no `team_id`, no `grant_team_editor`/`grant_team_viewer`, no `credit_cap`). Touches the whole `onboarding/team/[inviteId]/*` flow (profile, invite-ui, join, confirm, layout) and `context/team-invite-onboarding-context.tsx`. Independent of A3b — can run before or after it.
7. **A5 — `useOrg()` context and the role ladder.** Drop `teams`, `teamsLoading`, `refreshTeams`, `removeTeam`, `activeTeamId`/`setActiveTeamId` from `org-context.tsx` (only safe once A3b's consumers no longer need them). Collapse `roles.ts`'s `Member → Editor → Admin → Owner` ladder to `Member → Admin → Owner` — confirmed safe: `resolveRole()` has one call site (`org-context.tsx:289`), always called with empty grants, so `Editor` is unreachable today, not a live bug. `RoleBadge`'s `editor` variant (custom SVG glyph, color tokens, label) becomes dead once no member can hold that role — remove it there too, and in `MemberRow`'s `availableRoles`.
8. **A7 — Verify.** `npx tsc --noEmit`, manual smoke test (sidebar, project visibility toggle, chat share, invite flow, members page, settings nav).

**Recommendation: run A3c, A4, and A5 as separate sessions, each with its own commit and `tsc` pass.** A3c in particular is confirmed to be several times the size of A2 (deletes designed UI across ~6 files plus a full rewrite of a 3000-line page) — its connector-page flatten is a large, standalone effort on its own; don't bundle it with the TeamEditor-grant/persona-share cleanup in the same commit.

---

## 4. Thread B — Orphaned endpoint triage — ✅ confirmed dead, ready to execute

- `approveOverflow()` (`src/lib/api/organization.ts:458`, `POST /organizations/{id}/overflow/{requestId}/approve`) and its `OverflowResponseRaw.team_id` field are still live in the API layer, but **the backend route is confirmed gone** — there is no `overflow` path anywhere in `services/organizations/router.py`, and migration `a2f7d9c4e618_drop_dead_credit_layers` states outright that `OrganizationMember.credit_extra` was dropped because it "existed only to keep an older overflow response shape alive." No backend-dev confirmation needed anymore. **Action: remove `approveOverflow()` and every UI call site** (the "Approve credit overflow" action referenced in `role-guardrails.md` §6) rather than leave a control pointed at a route that 404s.

---

## 5. Thread C — Doc hygiene

- `role-guardrails.md` (2026-06-18) is the most detailed status doc for this area but is now confirmed stale on at least two points (§1). Rather than trust it wholesale for the remaining claims this map didn't independently verify (e.g. Admin's billing-surface scope, the `useWorkspaceRole()` hook), spot-check each against current source before acting on it.
- Once Thread A ships, retire `team-to-org-flattening-migration-plan.md` (fold its "done" state into this map) so there's one live document instead of two.

---

## 6. Out of scope — don't build yet, blocked on the back-end

These are real spec gaps (Part I of the compliance audit), but they're back-end gaps first — building front-end for them now would mean redoing the work once the back-end catches up:

- Credit-batch/rollover/expiry UI, Owner-set spend-cap control, top-up 20%-premium messaging, 8-tier Souvenir Core plan picker — the back-end has none of the batch/spend-cap model yet; there's nothing to wire against.
- A per-member invite list for "Shared" project visibility — the back-end broadcasts org-wide today. A `ProjectMember` grant table exists in the schema, but confirm it's actually the enforced visibility gate (not just a data table) before building UI on top of it.

---

## 7. Sequencing recommendation

Threads B and C are small, independent, and don't block on Thread A — start them first while the Thread A endpoint-path question is being confirmed with the backend dev. Thread A's Phase 1 is the gating step for everything after it; Phases 2–4 can't start until the data layer lands.
