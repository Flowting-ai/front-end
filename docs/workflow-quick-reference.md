# Workflow Builder - Quick Reference Card

## 🎨 Minimap Colors

**Node Types are color-coded:**
- � **Documents** = Gold
- 🟡 **Chats** = Gold
- 🟡 **Pins** = Gold
- 🔵 **Agents** = Blue
- 🔵 **Models** = Blue

**Usage:** Click anywhere on minimap to navigate there!

---

## ⌨️ Keyboard Shortcuts

| Key | Action |
|-----|--------|
| `Delete` or `Backspace` | Delete selected node or connection |
| `Alt + Drag` | Duplicate node while moving |
| `Shift + Click` | Multi-select nodes |
| `Ctrl/Cmd + Z` | Undo |
| `Ctrl/Cmd + Shift + Z` | Redo |
| `Ctrl/Cmd + S` | Save workflow |

---

## 🎯 Quick Actions

### On Node Hover (Top-Right Corner)
- **✏️ Edit** → Opens inspector panel
- **📄 Duplicate** → Creates instant copy

### Right-Click Menu
**On Canvas:**
- Add Node → (submenu with all types)

**On Node:**
- Duplicate
- Delete Node
- Group (coming soon)

**On Connection:**
- Delete Connection

---

## 🗑️ Delete Methods

### Nodes
1. Select → `Delete` key
2. Right-click → "Delete Node"
3. Inspector → "Delete Node" button (bottom)

### Connections
1. Click connection → `Delete` key
2. Right-click connection → "Delete Connection"

---

## 🔧 Utility Controls (Bottom-Center)

From left to right:
1. ↶ **Undo**
2. ↷ **Redo**
3. ➕ **Zoom In**
4. ➖ **Zoom Out**
5. ⊡ **Fit View**
6. 🗺️ **Toggle Minimap** (blue when active)
7. 💾 **Save**
8. 📁 **Load**
9. 🗑️ **Clear All**

---

## 💡 Pro Tips

✨ **Fast Duplication:** Hover → Click duplicate icon  
✨ **Alt+Drag:** Hold Alt while dragging to duplicate  
✨ **Quick Delete:** Just press Delete/Backspace  
✨ **Minimap Navigation:** Click minimap to jump around  
✨ **Connection Delete:** Click the line first, then Delete  
✨ **Multi-Delete:** Select multiple, then Delete once  

---

## 🎨 Visual Indicators

### Node Border Colors
- **Blue** = Selected
- **Gray** = Normal
- **Blue (on hover)** = Hovering

### Status Badges
- **Idle** = Gray background
- **Running** = Blue background  
- **Success** = Green background
- **Error** = Red background

### Ports
- **Blue (left)** = Input port
- **Green (right)** = Output port

---

## 🚀 Workflow Tips

1. **Start Simple**: Add 2-3 nodes first
2. **Connect**: Drag from output (right) to input (left)
3. **Configure**: Click node → edit in inspector
4. **Save Often**: Use Ctrl+S or auto-save (30s)
5. **Use Minimap**: Toggle on for large workflows
6. **Duplicate Smart**: Alt+Drag is fastest
7. **Clean Up**: Delete unused nodes/connections

---

## 📱 Best Practices

✅ **Name your nodes** descriptively  
✅ **Use descriptions** to document logic  
✅ **Save frequently** (or rely on auto-save)  
✅ **Test as you build** with Test Workflow button  
✅ **Keep minimap on** for navigation  
✅ **Use hover actions** for speed  
✅ **Organize spatially** - group related nodes  

---

## 🆘 Troubleshooting

**Can't delete?**  
→ Make sure you're not typing in an input field

**Hover actions not showing?**  
→ Move mouse over the node card itself

**Minimap disappeared?**  
→ Click the Map icon in utility section to toggle it back

**Connection won't delete?**  
→ Click the connection line first to select it, then press Delete

**Alt+Drag not working?**  
→ Make sure you're holding Alt BEFORE you start dragging

---

**Need more help?** Check the full documentation in `/docs/`
